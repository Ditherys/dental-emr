const extByMime:Record<string,string>={"image/jpeg":"jpg","image/png":"png","image/webp":"webp"};
export function sanitizeDisplayFilename(value:string,mimeType:string){if(!extByMime[mimeType]||value.length<1||value.length>255||/[\\/\0]|\.\.|[<>:"|?*]/.test(value)||value.trim()!==value) throw new Error("INVALID_FILENAME"); const base=value.replace(/\.[^.]+$/i,""); if(!/^[\p{L}\p{N}][\p{L}\p{N} _-]*$/u.test(base)) throw new Error("INVALID_FILENAME"); return `${base}.${extByMime[mimeType]}`;}
export function proposeDisplayFilename(input:{captureDate:string;category:string;toothCodes:string[];sequence:number;extension:string}){if(!/^\d{4}-\d{2}-\d{2}$/.test(input.captureDate)||!Number.isInteger(input.sequence)||input.sequence<1||!/^(?:jpe?g|png|webp)$/i.test(input.extension)) throw new Error("INVALID_FILENAME"); const cat=input.category.toLowerCase(); const teeth=input.toothCodes.length?"_tooth-"+input.toothCodes.map((x)=>x.replace(/[^a-zA-Z0-9-]/g,"")).join("-"):""; return input.captureDate+"_"+cat+teeth+"_"+String(input.sequence).padStart(2,"0")+"."+input.extension.toLowerCase();}
