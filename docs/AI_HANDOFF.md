# AI_HANDOFF.md

> Rolling handoff between coding agents. Keep this concise. The repository, approved plans, migrations, tests, ADRs, and Git history remain authoritative.

## Current Checkpoint

**Task / slice:** Codex review remediation of `661796f` (statement-boundary grace classifier) — still authored-only, not executed against any database. TEST-01 disposal/TEST-02 provisioning status is unchanged this checkpoint; see below.

**Codex review of `661796f` and remediation (2026-08-17):** Codex found one Low issue, addressed this checkpoint; no Critical/High/Medium findings.

- **Low — sibling candidates for grace, and the "newly added" set itself, were derived relative to the platform baseline (minus whatever was already pending), not relative to the immediately preceding statement.** `assertPreFinalStatementBoundary` computed `newlyAdded` as "added vs. baseline, and not already in `pending`." An entry that failed grace at some earlier statement is a real violation, is reported once, and is correctly never added to `pending` — but it remains present in every later snapshot (nothing revoked it) and therefore remains present in the baseline-relative `added` set forever. Because it was never pending, it kept reappearing in `newlyAdded` at every later boundary, available both as a spurious "still not fixed" re-check and — the concrete risk Codex demonstrated — as a false correlated PUBLIC sibling if a later, unrelated statement happened to `CREATE OR REPLACE` the very same object: the stale rows would satisfy `isKnownCreationDefaultPrivilege`'s sibling/object-identity check and get waved into `pending` as if they were that statement's own fresh default, disappearing from that boundary's report. Codex's synthetic repro: an unrelated statement produces 3 immediate violations on function `q()`; a later, snapshot-unchanged `CREATE OR REPLACE FUNCTION q()` then re-grants grace to all 3 with 0 problems reported at that boundary. Fixed: `assertPreFinalStatementBoundary` now takes `previousSnapshot` (the actual snapshot immediately preceding the one being judged) and derives both the lingering-pending check's `newlyAdded`/sibling-candidate set from `diffAgainstBaseline(previousSnapshot, snapshot)` — genuinely new since the last statement — instead of "not already pending." `scripts/run-boundary-privilege-invariant.mjs`'s `assertStatementModeFile` already tracked a `previous` cursor per statement (previously used only for `assertExaminedGrowth`); it now also threads that same cursor into `assertPreFinalStatementBoundary` as `previousSnapshot`, including across its own file boundary (deliberately — see below), so no new bookkeeping was needed at the runner level.
- **Consequence for the existing per-file grace-reset test, addressed rather than left inconsistent:** with `previousSnapshot` now the real preceding snapshot (which spans file boundaries, unlike `pendingGrace`, which still resets per file by design), a privilege left open at one file's end is no longer "newly added" at the next file's first statement — it already existed before that file started. This is correct, not a regression: the entry was already reported once, at its true first appearance, and remains independently caught at every file boundary by the caller's own ungraced `assertPreFinalBoundary` (unchanged, proven by the neighboring test). Re-flagging it a second time inside the next file's internal statement check would itself have been the kind of memory-less re-reporting the "report once, at first appearance" design principle argues against. Updated the affected test's expectations and renamed it to state what it now actually proves (no `pendingGrace` leak across files, distinguished from a genuine leak's "adjacent" message) rather than the outdated "fresh immediate violation" framing.
- Added tests: the exact repro Codex described (unrelated-statement violation on `q()`, then a same-object `CREATE OR REPLACE` with an unchanged snapshot, asserting the old rows are not re-admitted to `pending`); and an explicit cross-object case (`CREATE FUNCTION y()` whose statement diff also contains an unrelated `PUBLIC` row on a different function `z()` and an `authenticated` row on `y()` with no correlated `PUBLIC` sibling for `y()`) asserting neither is graced.
- 153/153 script tests (was 151), 265/265 full unit suite, lint, and typecheck all pass. Diff confined to `scripts/boundary-privilege-invariant.mjs`, `scripts/boundary-privilege-invariant.test.mjs`, `scripts/run-boundary-privilege-invariant.mjs`.

Still true, unchanged by this checkpoint: none of this tooling has ever executed against a real database. The table-defaults gap (Supabase's `ALTER DEFAULT PRIVILEGES` on new tables) remains a known, documented limitation, to be closed by measurement once R6-D first runs against TEST-02, not by static extension now.

---

**Prior checkpoint task / slice:** Codex review remediation of `4d3c66d` (statement-boundary grace classifier) — still authored-only, not executed against any database. TEST-01 disposal/TEST-02 provisioning status is unchanged that checkpoint; see below.

**Codex review of `4d3c66d` and remediation (2026-08-17):** Codex found one Medium and one Low issue, both addressed this checkpoint.

- **Medium — the grace classifier accepted each `anon`/`authenticated` EXECUTE row independently, without requiring the same snapshot to also contain the correlated PUBLIC EXECUTE row for the same object.** An effective `anon`/`authenticated` privilege does not by itself prove PUBLIC was its source — PostgreSQL also permits a direct GRANT to those roles, role-inheritance effects, or `ALTER DEFAULT PRIVILEGES` scoped to functions/routines (not only tables). The prior code matched purely on shape (objectClass/grantee/privilege/object identity), so a real, unexplained `authenticated` grant landing at the same statement boundary as a qualifying CREATE would have been silently graced. Fixed: `isKnownCreationDefaultPrivilege` now takes the full set of privileges newly added at this statement (`newlyAddedEntries` in `assertPreFinalStatementBoundary`) and, for any `anon`/`authenticated` entry, requires that set to also contain the matching-object, matching-privilege PUBLIC row before granting grace. The `public` row itself still graces unconditionally (it is the PostgreSQL-constant fact). Added a regression test: an `authenticated` EXECUTE row shaped exactly like the statement's own default, but with no correlated PUBLIC row in the same diff, now fails immediately rather than being graced.
- **Low — the per-file grace-reset test's inline comments described the wrong failure mechanism.** The comments claimed a leaked `pendingGrace` would make the second file "go quiet" (silently pass). That is not how the state machine behaves: the privilege is still present in file two's snapshot, so a leaked pending entry is still reported — as an "adjacent" violation, not silence. The test itself was already sound (it asserts the real message does *not* contain "adjacent"), but the comment explaining *why* was inaccurate, which would mislead whoever next has to reason about a regression here. Rewrote the comments to state the true distinction (which violation message comes back, not whether one comes back), renamed the test accordingly, and added an explicit control assertion that directly exercises `assertPreFinalStatementBoundary` with file one's actual leaked pending state to show it produces the "adjacent" message — then asserts the real `assertStatementModeFile` call for file two does not match it.
- 151/151 script tests (was 150), lint, and typecheck all pass. Diff confined to `scripts/boundary-privilege-invariant.mjs` and `scripts/boundary-privilege-invariant.test.mjs`.
- Codex's other findings from this review (`prokind` output correctness, table-defaults being deliberately ungraced, probe-shaped tests not executing the real SQL) were confirmed as already-correct/already-documented and required no code change.

Still true, unchanged by this checkpoint: none of this tooling has ever executed against a real database. The table-defaults gap (Supabase's `ALTER DEFAULT PRIVILEGES` on new tables) remains a known, documented limitation, to be closed by measurement once R6-D first runs against TEST-02, not by static extension now.

---

**Prior checkpoint task / slice:** Codex review remediation of `6b61a32` (statement-boundary grace classifier) — still authored-only, not executed against any database. TEST-01 disposal/TEST-02 provisioning status is unchanged that checkpoint; see below.

**Codex review of `6b61a32` and remediation (2026-08-17):** Codex found one Medium and one Low issue in the just-narrowed grace classifier, both addressed that checkpoint.

- **Medium — `isKnownCreationDefaultPrivilege` did not match the probe's actual output shape.** The live probe (`boundary-privilege-snapshot.sql`) reports *effective* privileges via `has_function_privilege`, so PostgreSQL's single PUBLIC EXECUTE default on a new function/procedure surfaces as three rows, not one: `public`/execute, `anon`/execute, `authenticated`/execute (PUBLIC's grant is, by ACL semantics, every role's effective grant). `KNOWN_CREATION_DEFAULT_PRIVILEGES` only listed the `public` row, so a real run would have graced the `public` row and immediately failed on the `anon`/`authenticated` rows of the exact same default — a false violation on every function/procedure the baseline creates. Fixed by adding explicit `anon`/`authenticated` entries (function and procedure) to the list, with a doc comment explaining why this is a PostgreSQL ACL fact, not a new policy exception. Separately, the probe's `routines` CTE hard-coded `object_class = 'function'` for every `pg_proc` row, so a `CREATE PROCEDURE`'s default privilege could never match the classifier's `procedure` entries even after the above fix; the SQL now derives `object_class` from `prokind`, matching `migration-privilege-lint.mjs`'s classification of the migration text. **Not fixed, and documented as deliberately unfixed:** Codex also flagged that the classifier ignores Supabase's `ALTER DEFAULT PRIVILEGES` on newly created tables (ADR-017 §2/§4). That default is per-project and `FOR ROLE`-scoped — unlike the PostgreSQL-constant function default, its effective grantees depend on which role actually runs `supabase db push`, a fact ADR-017 §4 records as unverified until R6-D executes for real. Hardcoding a guess would violate this list's own "not a policy choice" contract and could mask a genuine leftover grant. Added an explicit comment recording this as a known gap to close by *measurement* (e.g. `pg_default_acl` for the creating role) once R6-D first runs against TEST-02, not by static extension now. Statement mode continues to fail closed on any newly observed table privilege until then — the intended behavior, not a regression.
- **Low — the per-file grace-reset test passed for the wrong reason.** The prior test's first file executed both CREATE and REVOKE, so its pending grace was already empty before the second file ran; the assertion would have passed identically even with a leaking `pendingGrace`. Rewrote it so file one ends with the default privilege still open (CREATE only, no REVOKE), confirmed structurally that `pendingGrace` is a fresh local (`let pendingGrace = []`) reinitialized on every `assertStatementModeFile` call — it cannot leak by construction — and updated the test to actually exercise that: the same privilege reappearing in an unrelated second file is now asserted to fail immediately as a fresh, non-adjacent violation.
- Added tests: probe-shaped 3-row grace-and-adjacent-revoke-closes-all-three; an anon/authenticated-shaped row that is *not* the statement's own object still fails immediately; `CREATE PROCEDURE` grace matched by `object_class` rather than assumed to be a function.
- 150/150 script tests (was 147), lint, and typecheck all pass. Diff confined to `scripts/boundary-privilege-invariant.mjs`, `scripts/boundary-privilege-invariant.test.mjs`, `supabase/verification/r6d/boundary-privilege-snapshot.sql`.

Still true, unchanged by this checkpoint: none of this tooling has ever executed against a real database. The table-defaults gap above is a known, documented limitation — not a defect masked as fixed — and should be the first thing scrutinized once R6-D finally runs against TEST-02.

---

**Prior checkpoint task / slice:** R6-D boundary-invariant tooling corrections (still authored-only, not executed against any database), plus disposal of TEST-01 to unblock provisioning TEST-02.

**Implementing agent:** Claude Code, at the project owner's direction to close the two known R6-D tooling defects recorded in the prior checkpoint (statement-boundary/default-privilege design; the live probe expecting pgTAP without provisioning it) so R6-D is no longer blocked on tooling corrections once TEST-01 disposal and TEST-02 provisioning are approved.

**What changed, and why:**

1. **Statement-boundary/default-privilege fix.** `scripts/boundary-privilege-invariant.mjs` gains `assertPreFinalStatementBoundary`, used only by `--mode=statement`'s inner per-statement loop in `scripts/run-boundary-privilege-invariant.mjs` for non-terminal files. ADR-017 §2 requires a revoke "adjacent to the CREATE" — the very next statement, since SQL cannot combine CREATE and REVOKE atomically. PostgreSQL grants EXECUTE on every new function to PUBLIC at the instant of CREATE, so without a grace window, statement mode would report that expected, one-statement-wide transient as a violation on every single function the baseline creates — not a real defect, but the previous tooling could not tell the difference between that and a genuine leftover grant. The new logic gives an added privilege exactly one statement of grace and reports a real violation only if it is *still* present the statement after — i.e. it verifies "adjacent" rather than assuming it, and cannot mask a grant that persists past the next statement. The unmodified, ungraced `assertPreFinalBoundary`/`assertFinalBoundary` still run at every file boundary (both modes), so nothing about the end-of-file invariant changed. 5 new unit tests cover it; 133/133 script tests, lint, and typecheck pass.
2. **pgTAP-not-provisioned fix.** `live-authorization-probe.sql` requires pgTAP, which ADR-018 deliberately keeps out of the canonical baseline — it was previously only ever provisioned by manually remembering to run `npm run db:provision:test` first, with no enforcement. Added `supabase/verification/r6d/pgtap-presence-check.sql` (read-only) and `assertPgtapIsProvisioned` in `scripts/remote-database-test-guard.mjs`, called by the R6-D runner immediately after environment validation, before any migration is replayed. A skipped provisioning step now fails fast with the exact remedy instead of failing deep inside the live probe after the whole baseline has already been replayed. `supabase/verification/r6d/README.md` now documents the required provisioning step as "Step 0."

**Explicitly not done in this checkpoint:** the R6-D tooling is still authored-only and has never touched a database — none of the above is empirically verified, only unit-tested against synthetic snapshots. ADR-017/ADR-018 are unchanged; only the tooling and its README were corrected. This checkpoint does not run R6-C, R6-D, or R6-F, and does not touch DEV.

**TEST-01 disposal:** the project owner approved deleting `dental-emr-test-01` (ref `agxousnjhnzbnqnbvgbd`) now that its H-6 evidence is fully captured — see the prior checkpoint. Claude Code's attempt to run `npx supabase projects delete agxousnjhnzbnqnbvgbd` was refused by the local auto-mode permission classifier as too high-risk for an agent to execute unattended; **the project owner must run it manually** (`supabase projects list` confirms the two live projects and their distinct refs; `dental-emr-dev` ref `hjcmnmigvzufhvamlnmy` is untouched). This is the last blocker before TEST-02 can be created (free-tier two-project cap).

**Status carried forward, unchanged this checkpoint:** database layer, hosted Auth, catalog-level schema equivalence, and the browser matrices remain **verified green**; TEST-01 Playwright acceptance is **54/54**; H-6 is **fully resolved**. DEV migration history is still unreconciled and the migration freeze is still **ACTIVE**.

**Physical-device/assistive-technology QA pass (2026-08-15, same day, after the emulator-assisted pass):** the project owner personally exercised every remaining H-6 checklist item on real hardware, closing every item the emulator-assisted pass could not: the full phone checklist P1–P9 (via an ngrok tunnel from a physical phone to the local dev server), iPad T3 (physical attached/Bluetooth keyboard), desktop D1/D2 (real native Tab traversal and focus-ring visibility), D4 (native 200% browser zoom), and D5/X2 (a real screen reader — Narrator/NVDA — confirmed correct announcement of headings, landmarks, form labels, and validation errors, and, for X2 specifically, automatically announced the inline `role="status"` success message from a genuine Add Branch submission). All items passed with no defects found. The X2 check performed the first genuine mutation of this evidence file's QA history — one new synthetic branch was created in TEST-01 and nothing else changed. One transient, non-defect event was observed and recorded: MFA verification on the physical phone briefly surfaced `"Unable to verify the session security level."` (thrown server-side from `src/lib/auth/mfa.ts`'s `getAuthenticatorAssuranceLevel` check, unrelated to the ngrok tunnel) and cleared on retry, consistent with the shared TEST identity being used concurrently by automated and manual testing. Full detail in `docs/evidence/TEST-01-responsive-accessibility-manual-qa-2026-08-15.md`.

**Codex manual-QA remediation (2026-08-15):** Workforce invitation validation now uses `databaseUuid` for organization, role, and optional branch identifiers; action tests cover PostgreSQL-valid non-versioned IDs and malformed IDs before authorization/persistence. A hosted, non-mutating invitation validation check passed 1/1 and created no invitation. Coarse-pointer CSS now gives text-like native form controls and all interactive dropdown row variants a 44 px minimum while retaining compact fine-pointer density. Playwright asserts the stable rendered target size (after Radix's opening scale transition). Updated responsive coverage passed 35/35 across phone-360, phone-430, iPad portrait/landscape, and desktop-responsive; lint, typecheck, unit 240/240, secret scan, and build also pass. Evidence: `docs/evidence/TEST-01-responsive-accessibility-manual-qa-2026-08-15.md`.

**Codex TEST-01 acceptance remediation (2026-08-15):** A fresh-ID run initially passed 52/54 and disproved the stale-fixture explanation for the mutation-after-suspension failure. The application RPC's live-membership check was sound; the test restored the admin in `finally` before the React server action completed. The test now waits for the exact refusal alert while the actor remains suspended and verifies branch absence directly through the TEST-only administrative harness before restoration. Hosted password sign-in retries once with bounded waits and sanitized failure output. Credential-bearing Playwright traces are disabled because traces retain typed passwords and authorization headers; subsequent runs cleared the old trace output. The responsive suite also now waits for BranchForm hydration before entering an orientation draft and waits for the mobile sheet fade-in to reach stable opacity before running axe. Targeted proof: session boundaries 5/5, iPad portrait orientation 3/3, iPad portrait navigation 5/5. Final proof: typecheck, lint, unit 235/235, and Playwright 54/54.

**Playwright suspension-flow cascade fix, now executed against TEST-01:** `session-boundaries.spec.ts`'s mutation-after-suspension test suspended `environment.owner` mid-test — the same identity every other spec file signs in as. Fixed by provisioning a dedicated `adminUser` fixture (`scripts/provision-e2e-identities.mjs`, new `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD`/`E2E_ADMIN_TOTP_SECRET`) from the seed's existing, previously-unused `org-a-admin` row, which already carries an organization-wide ADMIN assignment (ADMIN holds `branch.manage`, same as OWNER). No seed/migration change needed.

**First full-matrix run (54 tests, 6 default local workers): 24 passed, 30 failed.** Diagnosed to a small number of environmental causes, not app bugs:
- A stale `next dev` process left listening on port 3000 from an earlier attempt — `playwright.config.ts`'s `reuseExistingServer` silently reused it instead of starting fresh with the correct TEST-01 env, so the whole run was against the wrong server state. Killed manually; not a repo fix (inherent to local dev workflow).
- 6-way local parallelism hammering one shared, MFA-gated owner identity against hosted (rate-limited) Supabase Auth caused cascading `"Unable to verify the session security level"` server errors and even a browser session closing mid-test. Fixed: `playwright.config.ts` now sets `workers: 1` — the suite's own `login.ts` retry logic already assumed serial execution, the config just never enforced it.
- WebKit browser binary was never installed locally (`ipad`, `ipad-portrait`, `ipad-landscape` all failed with "Executable doesn't exist"). Fixed by running `npx playwright install webkit chromium`.

**Second run, same env, with those three fixes: 42 passed, 12 failed.** Reading the saved `test-results/**/error-context.md` artifacts (screenshots + accessible-tree snapshots Playwright keeps after a run) rather than re-running blind, found four more root causes, all in the *test harness*, not the app:
1. `expectUsableTargets`'s `measure()` in `responsive-accessibility.spec.ts` only detected explicit `label[for]` association; `branch-form.tsx`'s "Visible on the public website" checkbox uses implicit `<label>` wrapping (a real, well-designed ≥44px target) that the check never saw, reporting a false 16×16 violation. Fixed: also check `element.closest("label")`.
2. `isNarrow` in the same file hardcoded `width < 1024` to decide "collapsed nav," but the app's real breakpoint (`emr-shell.tsx`/`mobile-navigation.tsx`) is Tailwind's `xl:` = 1280px. iPad landscape (1194px) falls in the gap — collapsed in the app, "desktop" per the old test threshold. Fixed: match `1280`.
3. The org name (`branchContext.organization.name`) renders **twice** in `emr-shell.tsx` — an `xl:`-only sidebar copy and a `sm:block xl:hidden` header copy — so it's always visible somewhere below `xl`, but `foundation.spec.ts`/`session-boundaries.spec.ts` asserted `getByText(name).first()`, which picks by DOM order, not visibility. Below `xl` the (CSS-hidden) sidebar copy is first, so the assertion failed exactly where it started actually being exercised (previously only ever run on desktop, where the visible copy happens to be first). Fixed: `.filter({ visible: true }).first()` at all three call sites.
4. `signInOwnerWithTotp`'s documented worst-case TOTP-collision retry can take ~30s by itself; Playwright's default test timeout is also 30s, so a collision alone could exhaust a test's entire budget even when every actual step it performed would have succeeded (confirmed from a saved snapshot: login had succeeded and the branch-selector interaction had already completed correctly by the time the test was killed for timing out). Fixed: `playwright.config.ts` now sets `timeout: 90_000`, covering the bounded password retry plus one TOTP-window retry.

The clean fresh-ID rerun is complete. The duplicate Branch A3 failure was stale fixture data. The mutation-after-suspension failure was a test race, not evidence of an authorization bypass; its corrected terminal-state and direct-database assertions pass. Keep generating `E2E_RUN_ID` in-process for every local run because the external env file still contains a static fallback.

**H-1 (catalog equivalence) is now resolved** — see `docs/evidence/R6E-catalog-comparison.md`. A schema-only `pg_dump` of `public`/`private`/`extensions` from both `dental-emr-dev` and `dental-emr-test-01`, over Supabase's Session Pooler, came back byte-for-byte identical (128,294 bytes each) apart from pg_dump's own random per-run `\restrict`/`\unrestrict` token. `supabase link` was never issued against DEV during this — the method used a direct `pg_dump` connection string instead, so the freeze's core concern (an accidental migration-applying command against DEV) never had a path to occur.

**Deviation, recorded honestly:** the plan was for the project owner to run the DEV-side `pg_dump` personally so its password would stay out of Claude Code's view. In session, several of the owner's own attempts failed (a wrong assumed hostname, then password-propagation timing right after a reset), and the owner explicitly asked Claude Code to run both sides directly instead. Both DEV's and TEST-01's database passwords were consequently visible to Claude Code for this task. DEV's password was rotated once during troubleshooting (to get a working credential, at the owner's direction) and, per the owner's decision, was not rotated again afterward. Full detail in `docs/evidence/R6E-catalog-comparison.md`.

**Codex review of `506a4c1` and remediation (2026-08-17):** Codex reviewed the statement-boundary grace fix and pgTAP preflight from the prior checkpoint and found one Medium and one Low issue, both addressed this checkpoint, no code left un-remediated.

- **Medium — grace was not restricted to CREATE-derived default privileges.** `assertPreFinalStatementBoundary` gave one statement of grace to *any* newly observed, not-already-accepted privilege, not specifically the PostgreSQL-automatic PUBLIC EXECUTE default that CREATE FUNCTION/PROCEDURE produces. That meant an unexplained GRANT (or any other side effect) followed by an adjacent REVOKE/DROP would have passed silently — broader than the README's claimed "CREATE then adjacent REVOKE" exception, and exactly the kind of interruption window R6-D exists to catch. Fixed: the function now takes the classified statement that was just executed and grants grace only via `isKnownCreationDefaultPrivilege`, which requires all three — the statement is a resolved CREATE FUNCTION/PROCEDURE, the entry's canonical object identity matches the object that statement just created, and the (objectClass, grantee, privilege) triple is in the exhaustive, hard-coded `KNOWN_CREATION_DEFAULT_PRIVILEGES` list (PUBLIC EXECUTE on function/procedure — the only default PostgreSQL itself grants at CREATE time). Anything else newly observed now fails immediately, at the boundary it first appears, rather than waiting a statement. `scripts/run-boundary-privilege-invariant.mjs` was updated to classify and pass the executed statement.
- **Low — new fail-closed and orchestration paths lacked committed regression tests.** Added: two interleaved privilege-bearing CREATEs attributed to their own objects independently (confirming a mid-flight CREATE ends the first object's grace, per ADR-017's true "adjacent" meaning); an unexpected privilege that disappears next statement still fails immediately rather than getting grace; a default privilege that doesn't belong to the statement just executed also fails immediately; and full `assertPgtapIsProvisioned` coverage (malformed JSON, zero rows, extra rows, wrong column, wrong sentinel value, exact remedy text) — previously untested at all. The per-file `pendingGrace` reset was also previously untestable because it lived as a local variable inside `main()`'s I/O loop; extracted the per-file bookkeeping into a new pure, exported `assertStatementModeFile` (same behavior, no I/O, testable with precomputed snapshots) and added tests proving grace never carries across a file boundary and that an entry left open at a file's last statement is still caught by that file's own ungraced `assertPreFinalBoundary` check. 147/147 script tests (was 133), lint, and typecheck all pass; worktree stayed otherwise clean.

Still true, unchanged by this checkpoint: none of this tooling — including the pgTAP preflight or grace behavior — has ever executed against a real database. TEST-01 deletion and TEST-02 provisioning remain the separately approved next actions per the items below.

**Codex pre-push review findings (2026-08-15):** four concrete issues were found. (1) Local commit `fe96a1f` accidentally embedded a 63-variable environment dump in its commit message. No password/API-token-looking variable names were present, but workstation and session metadata were; the unpushed history was rewritten, producing sanitized commit `fa258b6`, and the outgoing commit bodies contain no `declare -x` dump. (2) The physical-phone QA left a personal ngrok hostname in `allowedDevOrigins`; it was removed before commit, restoring the committed localhost/127.0.0.1-only default. (3) fresh-project E2E provisioning enrolled both MFA factors before persisting either new secret, so a second-enrollment failure could strand the first verified factor; `2665de9` now persists each credential immediately and adds a unit test for first-write/later-append behavior. (4) this handoff contradicted itself about whether H-6 still needed TEST-01; the wording below now consistently says H-6 is complete and TEST-01 awaits an explicit preserve/dispose decision. Remaining review blockers were not guessed away: R6-D's statement-boundary/default-privilege design and pgTAP bootstrap path still need correction, and the Codex-authored checkpoint still needs independent non-author review.

## Environment state a new session must know

- **Git remote exists:** `Ditherys/dental-emr` (private). The `gh` CLI has two accounts; the active one must be `Ditherys` or pushes 404 — `gh auth switch --user Ditherys`.
- **TEST-01 (`dental-emr-test-01`) exists and is LINKED.** Region `ap-southeast-1`, Postgres `17.6.1.155` — identical to DEV, which mattered for the now-complete equivalence comparison. It holds the eight baseline migrations, pgTAP, the synthetic seed, four provisioned login identities including verified owner and admin TOTP factors, and one additional synthetic branch created by the X2 screen-reader check. H-6 evidence is complete; preserve or dispose of TEST-01 only by explicit owner decision. **The project owner is on Supabase's free plan — a hard cap of two projects at a time (DEV + one TEST), not just a runbook convention** — so TEST-01 must be deleted (with approval) *before* TEST-02 can be created for R6-D; the two cannot coexist.
- **Credentials live OUTSIDE the repo** at `C:\Users\D_Reyes\.dental-emr\test.env` (bash-format `export` lines). Now includes `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD`/`E2E_ADMIN_TOTP_SECRET`. **`E2E_RUN_ID` is a static value in this file** — regenerate it before every local re-run (CI does this automatically per-run; the local workflow does not) or fixture names collide with the previous run's leftover data. `scripts/load-test-env.ps1` (new this checkpoint) loads this bash-format file into a PowerShell session — must be re-run in every new window. Separately, `.env.local` inside the repo points at **DEV** for `npm run dev`.
- **The migration freeze is ACTIVE.** Guarded commands against TEST need the scoped `MIGRATION_FREEZE_ACK` / `MIGRATION_FREEZE_ACK_COMMAND` pair. Nothing may run against DEV.

## What is now proven

- **R6-C:** the eight-file baseline builds the complete Phase 1 schema on an empty project — no manual step, no superseded migration.
- **R6-E:** 6/6 pgTAP suites pass, generated types show no drift, schema lint clean, advisors 0 errors. The four suites written against the *superseded* thirteen-migration chain pass unmodified against the baseline — behavioural evidence the consolidation preserved semantics. **Now also a full catalog-level equivalence proof:** a schema-only `pg_dump` of `public`/`private`/`extensions` from DEV and TEST-01 is byte-for-byte identical. See `docs/evidence/R6E-catalog-comparison.md`.
- **Hosted Auth (R4 / H-7):** TEST-01 and DEV both report 14 passed, 0 violations, 0 unverified, 1 advisory. All 15 policy key names resolved against the live Management API.
- **CI:** `Application verification` is green on `main`.

## Defects found by executing things for the first time

Five application defects, none visible to any static check, unit test, or database suite. All fixed with regression coverage — see `docs/evidence/R6C-R6E-test01.md` and `docs/evidence/CI-first-runs.md`.

1. `z.uuid()` rejected valid PostgreSQL UUIDs, returning 500 for every member of the seeded organization. Fixed with `databaseUuid` (`z.guid()`).
2. The Next 16 dev server 403'd its own `/_next` chunks over `127.0.0.1`, so nothing hydrated and no interactive E2E flow could ever have passed. Fixed with `allowedDevOrigins`.
3. A pre-hydration click submitted the MFA forms natively as GET, putting a one-time TOTP code into the URL, history, `Referer`, and access logs. Fixed with `method="post"` plus a `useHydrated()` gate.
4. The seed left GoTrue token columns NULL, breaking Supabase's Admin API for the entire project. Fixed in the seed with a repairing `on conflict`, asserted in pgTAP.
5. `tsc --noEmit` passed locally off a stale `.next/` and failed in CI. Fixed by typing the root layout explicitly.

## Open items, in dependency order

| Item | State |
|---|---|
| **H-1** catalog-level TEST-vs-DEV equivalence | **Resolved 2026-08-14.** See `docs/evidence/R6E-catalog-comparison.md`. |
| **H-2** — R6-D on a fresh TEST-02, then R6-F reconciliation and freeze removal | **R6-D tooling corrections complete this checkpoint** (statement-boundary/default-privilege grace window; pgTAP preflight) — see above. Still blocked on: (1) the project owner manually deleting TEST-01 (command staged, refused by the auto-mode classifier for Claude to run itself), (2) creating TEST-02 from the disposed slot, (3) approving R6-D execution itself. None of the corrected tooling has run against a real database yet. R6-F (the only step that writes to DEV — a migration-history repair) remains gated on a green R6-D and needs its own separate approval. |
| Playwright harness sequencing | **Resolved 2026-08-15.** Fresh-ID TEST-01 acceptance run passed 54/54; the corrected suspension mutation test also proves direct database absence before membership restoration. |
| Responsive matrix on the other four form factors | **Resolved 2026-08-15.** All seven configured projects passed in one 54/54 matrix; the later manual device/accessibility pass is also complete under H-6 below. |
| **H-5** branch update/archive | unimplemented; needs migration 9, deliberately deferred until after R6-F |
| **H-6** manual responsive/accessibility pass | **Resolved 2026-08-15.** Emulator-assisted evidence plus a full physical-device/assistive-technology pass (phone, iPad, desktop keyboard traversal, 200% zoom, real screen reader) are all recorded at `docs/evidence/TEST-01-responsive-accessibility-manual-qa-2026-08-15.md`; QA-01 and QA-02 are fixed and physically confirmed. |
| **M-5** leaked-password protection | Pro-gated; a production gate, unsatisfiable on the current plan |
| **M-6** CodeQL + Dependency review | need GitHub Advanced Security on a private repo; left honestly red, no `continue-on-error` |
| Cloud TEST CI job | fails on unconfigured `cloud-test` environment variables, and would fail at the freeze regardless |
| **H-8** independent review | still required for every commit listed below; the latest Codex-authored checkpoint requires Claude or another non-author pass |

## Human actions still required

1. Configure the GitHub `cloud-test` environment (variables and secrets per `docs/deployment/CI_FOUNDATION.md`) — only useful after R6-F.
2. **Decided 2026-08-14:** the project owner will stay on the current Supabase plan until there is real clinic demand (a second clinic interested), and upgrade then. M-5 (leaked-password protection) remains open as a pre-production gate, not a Phase 1 blocker.
3. **Delete `dental-emr-test-01` manually** — approved 2026-08-17, but the auto-mode permission classifier refused to let Claude Code run the delete itself: `npx supabase projects delete agxousnjhnzbnqnbvgbd` (or via the Supabase dashboard). It contains one additional synthetic branch created by the X2 screen-reader check; that's already captured in evidence and disposable. `dental-emr-dev` (ref `hjcmnmigvzufhvamlnmy`) is a different project and is not affected.
4. After TEST-01 is gone: approve creating TEST-02 and approve R6-D execution against it (both still separately gated per ADR-017).

## Next Checkpoint

R6-D's statement-boundary/default-privilege grace classifier has now been through four rounds of Codex review (`506a4c1`, `6b61a32`, `4d3c66d`, `661796f`) with every finding addressed and no findings currently outstanding; the pgTAP preflight is likewise corrected. None of it has executed against a real database yet. This latest checkpoint still needs its own independent non-author review (see the CODEX REVIEW PROMPT below), and the earlier Codex-authored checkpoint `2665de9` still needs independent non-author review too. Next, once this checkpoint is reviewed clean: the project owner deletes TEST-01 manually (human action 3 above), then TEST-02 can be created and R6-D run against it for the first time — the true first empirical test of this tooling, including the documented table-defaults gap. Only a corrected, green R6-D may gate R6-F (DEV migration-history reconciliation and freeze removal), which requires its own separate approval. `docs/PHASE1_ACCEPTANCE_REVIEW.md` remains a historical review at an older checkpoint and must not be treated as the current status; rerun/update acceptance after the remaining gates close.

## Commits Requiring Later Codex Review

HIGH unless noted:

`e790ffe` R6-C1 baseline + guard change, `37ef684` R5-A session-boundary suite, `afb5518` R5-B withdrawal harness (introduces secret-key use in the test process), `3f2c658` R9-A matrix (MEDIUM), `1c92e8a` R4 hosted-Auth verifier (MEDIUM), `65545c7` MFA reconciliation docs (LOW), `d6cf076` acceptance review (LOW), `a5f15d1` R6-C/E execution, `6cdbff1` typecheck + CI evidence, `388af72` environment-scoped HIBP rule, `b25d2e3` four application defect fixes, `d58a476` hosted Auth findings, `484cff5` hosted Auth resolution — **a deliberate hosted write; scrutinise the reasoning recorded in `HOSTED_AUTH_BASELINE.md`**.

Earlier and still unreviewed: `c2a6c91` R6-A secure baseline, `35092e7` R6-B grant-last enforcement.

`492bfa0` R6-E catalog-comparison evidence + H-1 resolution (LOW — docs-only, no code/schema change, but scrutinise the deviation disclosed above: DEV/TEST-01 passwords became visible to the implementing agent, and DEV's password was rotated once mid-session).

`0264ff2` Playwright suspension-flow cascade fix (MEDIUM — introduces a new organization-wide privileged E2E identity; scrutinise that `adminUser` genuinely can't reach anything `branchUser`-scoped tests assume it can't, and that no other spec file's ordering assumptions relied on the old owner-suspension behavior). Now executed against TEST-01 — see findings above.

`fa258b6` PowerShell test.env loader (LOW — dev convenience only, no credential material; commit message sanitized before push after a shell-expansion environment dump was detected locally).

`8c2dc89` first full-matrix harness fixes (MEDIUM — verify serial execution, visibility filtering, breakpoint alignment, and timeout assumptions); `365f29b` physical-device/assistive-technology evidence (LOW — docs/evidence only).

`2665de9` Final TEST-01 harness hardening plus QA-01/QA-02 remediation (HIGH — includes server-action validation, global coarse-pointer CSS, E2E/unit tests, config, and MFA-secret persistence hardening; no schema/migration change). Independently review the `databaseUuid` boundary, 44 px selectors and animation-stable measurement, server-action terminal-state synchronization and direct database absence assertion, bounded password retry, trace disabling, 90-second test budget, hydration-ready orientation check, stable-opacity axe scan, and immediate outside-repository MFA-secret persistence before acceptance. Hosted proof: original suite 54/54, updated responsive matrix 35/35 in bounded groups, non-mutating invitation regression 1/1.
